# Target environment: local | qa | staging | prod-like
TARGET_ENV=local

# Open-Meteo uses public API; optional overrides
OPEN_METEO_BASE_URL=https://api.open-meteo.com

# Stripe test mode (https://docs.stripe.com/api/authentication)
STRIPE_SECRET_KEY=
STRIPE_API_VERSION=2024-11-20.acacia

# Optional webhook secret for signature fixture tests (use a test value or real whsec_ for SDK-aligned tests)
STRIPE_WEBHOOK_SECRET=

# Plaid sandbox (https://plaid.com/docs/sandbox/)
PLAID_CLIENT_ID=
PLAID_SECRET=
PLAID_ENV=sandbox
PLAID_INSTITUTION_ID=ins_109508
PLAID_INITIAL_PRODUCTS=investments

# Alpaca paper (https://docs.alpaca.markets/us/docs/paper-trading)
ALPACA_API_KEY_ID=
ALPACA_API_SECRET_KEY=

# Coinbase Exchange (https://docs.cdp.coinbase.com/exchange/rest-api/authentication)
COINBASE_API_KEY=
COINBASE_API_SECRET=
COINBASE_PASSPHRASE=

# FRED (https://fred.stlouisfed.org/docs/api/fred/v2/api_key.html)
FRED_API_KEY=

# SEC — public files; set a descriptive User-Agent per https://www.sec.gov/developer
SEC_USER_AGENT=apiautomation/1.0 (+https://example.com/contact)

# Optional base URL overrides (advanced)
# STRIPE_BASE_URL=
# SEC_BASE_URL=

# Notifications — see docs/run-project-wizard.md and docs/notifications-setup.md
# Channel: email | teams | both | none  (use teams if SMTP is blocked)
NOTIFY_CHANNEL=teams
NOTIFY_ENABLED=false
NOTIFY_TO=swapnil_patil10@persistent.com
NOTIFY_FROM=
NOTIFY_ONLY_ON_FAILURE=true
# Shown in Teams/email as the Allure link (local viewer)
ALLURE_REPORT_URL=http://localhost:9292
# Email (SMTP)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
# Microsoft Teams — Power Automate HTTP trigger OR classic Incoming Webhook
# powerautomate | teams (auto-detected from URL if omitted)
TEAMS_WEBHOOK_KIND=powerautomate
TEAMS_WEBHOOK_URL=
